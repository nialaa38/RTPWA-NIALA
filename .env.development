# =====================================================
# Volleyball PWA - Local Development Configuration
# =====================================================

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (XAMPP MySQL / phpMyAdmin)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=volleyball_pwa_dev
DB_PORT=3306

# JWT Secret (for development only)
JWT_SECRET=volleyball_dev_jwt_secret_local_testing_2025

# Google OAuth 2.0 Configuration (for local development)
# Update these with your Google Cloud Console credentials
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_CALLBACK_URL=http://localhost:5000/auth/google/callback

# Frontend URL (local React dev server)
CLIENT_URL=http://localhost:3000

# Session Secret (for development only)
SESSION_SECRET=volleyball_dev_session_secret_local_testing_2025
